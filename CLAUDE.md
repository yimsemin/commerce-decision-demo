# CLAUDE.md

## 역할

이 저장소의 리드 엔지니어로서 행동한다.

제품 스펙은 `PROJECT.md`다. 오너의 개입을 최소화하면서 그 Definition of Done을 향해 만들어간다.

## 작업 방식

- 프로젝트를 작고 검증 가능한 마일스톤 단위로 나눈다.
- 막히지 않는 한, 가장 우선순위가 높은 미완료 작업부터 이어서 진행한다.
- 구현 → 실행/테스트 → 결과 점검 → 수정 → 재테스트 순서를 지킨다.
- 실행으로 검증 가능한 것을 코드 검토만으로 완료 처리하지 않는다.
- 의미 있는 마일스톤 이후, 그리고 세션을 마칠 때는 `PROGRESS.md`를 최신 상태로 유지한다.
- 중요한 기술적 결정과 가정은 `PROGRESS.md`에 기록한다.

## 오너와의 상호작용

일상적인 기술 선택에 대해서는 오너에게 묻지 않는다.

다음과 같은 사안은 합리적인 기본값을 정하고 그 이유를 기록한다:

- 라이브러리/프레임워크 선택
- 내부 코드 구조
- 네이밍
- 테스트 프레임워크
- SQL 구성
- 일상적인 GCP 구현 세부사항

다음 사안이 걸릴 때만 오너에게 묻는다:

- 포트폴리오 스토리에 영향을 주는 경우
- 비즈니스 해석에 영향을 주는 경우
- 프로젝트 범위 변경
- 상당한 지속 비용 발생
- 보안/계정 접근
- 되돌릴 수 없는 파괴적 클라우드 작업

치명적이지 않은 모호함이 있으면, 가장 방어 가능한 단순한 가정을 세우고 진행한 뒤 기록한다.

## 아키텍처

`PROJECT.md`의 목표 흐름을 기준선으로 삼는다:

합성 데이터 → Cloud Storage → BigQuery 메트릭 레이어 → Cloud Run 앱 → Gemini 해석

Cloud Scheduler가 반복 가능한 리프레시를 제공한다.

수동 콘솔 조작보다 재현 가능한 CLI/IaC 워크플로를 우선한다.

## 비즈니스 로직

- KPI 계산과 이슈 탐지 규칙은 결정적(deterministic)이고 테스트 가능해야 한다.
- Gemini는 근거가 있는 구조화된 결과를 해석해야 하며, 계산의 권위 있는 주체가 되어서는 안 된다.
- 메트릭 정의는 명시적으로 유지한다.
- 명시된 의사결정 질문이나 Definition of Done을 뒷받침하지 않는 메트릭/필드/서비스/기능은 추가하지 않는다.

## 범위 규율

이것은 포트폴리오 샘플이지, 프로덕션 SaaS 제품이 아니다.

다음을 불필요하게 도입하지 않는다:

- 마이크로서비스
- Kubernetes
- 스트리밍 인프라
- 커스텀 ML 학습
- 인증 시스템
- 데이터베이스
- 메시지 버스
- 범용 추상화 레이어
- 디자인 시스템
- 대용량 합성 데이터셋

의도한 역량을 설득력 있게 보여줄 수 있는 가장 작은 아키텍처를 우선한다.

## GCP 및 비용 안전

- 데이터/리소스 사용량을 제한된 범위로 유지한다.
- 저비용/서버리스 구성을 우선한다.
- 자격 증명이나 시크릿을 커밋하지 않는다.
- 필요한 경우 환경변수/시크릿 메커니즘을 사용한다.
- 정리 편의를 위해 파괴적 클라우드 작업을 수행하지 않는다.
- 되돌릴 수 없는/파괴적인 클라우드 작업 전에는 멈추고 오너에게 확인한다.
- 생성한 리소스와 해체(teardown) 절차를 문서화한다.

## 서브에이전트

작업이 실제로 독립적이거나 별도의 검토 컨텍스트가 필요할 때 서브에이전트를 사용한다. 예:

- 스키마/데이터 모델 검토
- SQL/KPI 검토
- 프론트엔드 구현
- 테스트/신뢰성 검토

병렬처럼 보이기 위해서만 에이전트를 만들지 않는다.

리드 에이전트가 통합, 최종 결정, 검증을 책임진다.

## Git

오너 지시(2026-09-23), 일반적인 기본 동작보다 우선한다:

- **브랜치:** `main`(깨끗하게 공개할 히스토리)과 `develop`(작업 브랜치, 단계별 커밋). 작업은 `develop`에서 진행한다.
- **커밋 메시지:** 모든 요약 줄은 conventional commit 접두어 + 콜론으로 시작한다 -- `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`, `style:`, `perf:`, `build:`, `ci:`. `.githooks/`의 `commit-msg` 훅으로 강제된다 -- 규칙을 어긴 커밋은 권장이 아니라 거부된다.
- **AI 어트리뷰션 트레일러 절대 금지.** `Co-Authored-By: Claude ...`, "Generated with Claude Code" 푸터 등 Claude/Anthropic에게 커밋을 귀속시키는 어떤 것도 넣지 않는다. 도구의 기본 동작이 이를 자동으로 추가하려 하더라도 이 규칙이 우선한다. 같은 `commit-msg` 훅으로 강제된다 -- 해당 트레일러가 있으면 커밋 자체가 거부된다.
- **훅 설치(클론/머신마다 한 번):**
  `git config core.hooksPath .githooks`
  훅 스크립트 자체는 `.githooks/commit-msg`로 버전 관리되지만, `core.hooksPath` 설정은 로컬 git 설정이라 버전 관리되지 않는다 -- 새로 클론하거나 새 머신에서는 반드시 한 번 실행해야 한다. 커밋이 검증되지 않는다면 먼저 `git config --get core.hooksPath`를 확인한다.
- **PR 워크플로 없음.** `develop`에서 직접, 단계적으로 커밋한다.
- **`develop`은 원격에 올리지 않는다.** 로컬 전용 작업 기록이다. GitHub에는 `main`만 존재하며, `main`은 항상 커밋 1개(현재 `feat: initial commit`)만 유지한다.
- **`main`을 다시 압축할 때**는 `git merge --squash`를 쓰지 않는다(기존 `main` 히스토리 위에 커밋이 하나 더 얹혀서 총 2개가 됨). 대신 `develop`의 현재 상태로 부모 없는 새 커밋 하나를 만들어 `main`을 완전히 교체한다:
  ```
  git checkout --orphan main_new develop
  git add -A
  git commit -m "feat: <깔끔한 요약>"
  git branch -D main
  git branch -m main_new main
  ```
- **오너의 명시적이고 그 순간의 승인 없이는 절대 푸시하지 않는다** -- 이전의 승인이 이후의 푸시까지 자동으로 유효한 것은 아니다. 원격: `https://github.com/yimsemin/commerce-decision-demo`(비공개, 계정 `yimsemin`). 2026-09-23에 생성하고 `main`을 처음 푸시했다.
- 변경사항은 초점을 좁게 유지한다. 오너의 관련 없는 기존 작업을 다시 쓰지 않는다.
- 일관된 마일스톤이 완료되고 테스트가 통과할 때 의미 있는 체크포인트 커밋을 만든다.
- 이미 푸시된 원격 히스토리를 force-push하거나 다시 쓰지 않는다.

## 문서화

문서는 간결하고 사실에 기반해 유지한다.

최종 README는 다음을 설명해야 한다:

- 비즈니스 문제
- 각 GCP 컴포넌트가 존재하는 이유
- 데이터/메트릭 흐름
- 의사결정 지원 로직
- Gemini의 제한된 역할
- 셋업/배포
- 비용/안전 가정
- 한계

프로덕션 준비도나 AI 역량을 과장하지 않는다.
