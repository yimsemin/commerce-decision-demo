# GCP 커머스 경영 의사결정 랩

## 1. 프로젝트 목적

기획/전략 직무가 파편화된 커머스 데이터를 경영 의사결정의 근거로 바꾸는 과정을 보여주는, 포트폴리오 수준의 Google Cloud 샘플 시스템을 구축한다.

이것은 **프로덕션 커머스 플랫폼이 아니며**, 실제 회사/고객 데이터를 사용하지 않는다. 모든 비즈니스 데이터는 합성(synthetic) 데이터여야 한다.

시스템은 다음 전체 흐름을 보여줘야 한다:

**합성 비즈니스 데이터 → 클라우드 적재 → 통합 메트릭 → 이슈 탐지 → 의사결정 지원 → 사후 검증**

이 프로젝트의 가치는 화려한 대시보드 자체가 아니다. 가치는 계산 과정이 추적 가능하고 재현 가능한, 일관된 의사결정 시스템을 보여주는 데 있다.

---

## 2. 시나리오

다음을 운영하는 가상의 회사를 가정한다:

- 여러 브랜드
- 여러 판매 채널
- 여러 채널에 걸친 유료 마케팅
- 고객 획득 및 재구매 활동
- SKU 단위 재고

원본 데이터는 성장하는 커머스 기업이 실제로 그렇듯 의도적으로 파편화되어 있다.

주요 사용자:

- 경영진 / 사업전략 / 사업운영 담당자
- 무엇이 바뀌었는지, 왜 바뀌었는지, 무엇에 주목해야 하는지, 어떤 조치를 고려해야 하는지를 빠르게 파악해야 함

---

## 3. 시스템이 답해야 할 핵심 질문

최소한 이 애플리케이션은 다음 질문에 답하는 데 도움이 되어야 한다:

1. 매출과 주문 수에서 실질적으로 무엇이 바뀌었는가?
2. 어떤 브랜드/채널/제품이 그 변화에 가장 크게 기여했는가?
3. 마케팅 효율은 개선되고 있는가, 악화되고 있는가?
4. 고객 획득과 재구매 패턴은 변화하고 있는가?
5. 어떤 SKU가 품절 위험 또는 과잉재고 위험이 있는가?
6. 지금 경영진이 주목해야 할 이슈는 무엇인가?
7. 조치 또는 시뮬레이션된 개입 이후, 관련 KPI는 개선되었는가?

시스템은 다음을 구분해야 한다:

- **사실/계산된 메트릭**
- **규칙/이상 탐지**
- **AI가 생성한 해석**

Gemini는 KPI 계산의 근거(source of truth)로 취급되어서는 안 된다.

---

## 4. 목표 아키텍처

```text
합성 커머스 데이터 생성기
        ↓
Google Cloud Storage
        ↓
BigQuery
  - raw/staging
  - 통합 모델
  - KPI/메트릭 레이어
        ↓
Cloud Run
경영 의사결정 앱
        ↓
Vertex AI / Gemini
경영진 해석/설명

Cloud Scheduler
        ↓
예약된 합성 데이터 리프레시/파이프라인 실행
```

인프라와 배포는 커맨드라인 도구와, 실용적인 범위에서 Infrastructure as Code를 활용해 재현 가능해야 한다.

우선순위:

- `gcloud`
- Terraform
- SQL
- 반복 가능한 스크립트

수동 Google Cloud 콘솔 조작을 핵심 의존성으로 삼지 않는다.

---

## 5. 데이터 도메인

의사결정 사례에 충분하면서도 합성 모델은 간결하게 유지한다.

### 커머스(Commerce)

대표 필드 예시:

- 일시
- 주문 ID
- 브랜드
- 채널
- SKU/제품
- 수량
- 판매가
- 할인
- 순매출
- 고객 ID

### 마케팅(Marketing)

대표 필드 예시:

- 날짜
- 브랜드
- 캠페인/채널
- 지출
- 노출수
- 클릭수
- 기여 주문/매출

### 고객(Customer)

주문에서 포함되거나 파생될 수 있는 대표 필드:

- 고객 ID
- 획득 날짜/채널
- 최초 주문 여부
- 주문 이력

### 재고(Inventory)

대표 필드 예시:

- 날짜
- SKU
- 기초/보유 재고
- 입고
- 판매 수량
- 기말 재고

명시적인 메트릭이나 의사결정 사례에 필요한 필드만 추가한다.

---

## 6. KPI/메트릭 레이어

정확한 공식은 한 곳에 문서화하고 결정적으로 구현해야 한다.

최소한 다음 KPI 그룹이 필요하다:

### 매출(Sales)
- 순매출
- 주문 수
- 판매 수량
- 평균 주문 금액

### 마케팅(Marketing)
- 광고 지출
- 기여 매출
- ROAS
- CAC (합성 모델이 방어 가능한 계산을 지원하는 범위에서)

### 고객(Customer)
- 신규 vs 재구매 고객/주문
- 재구매 지표 또는 비율

### 재고(Inventory)
- 보유 재고
- 최근 판매 속도
- 예상 재고 소진 일수(days of cover)
- 품절 위험
- 방어 가능한 범위에서의 과잉재고 신호

다음과 같은 비교를 지원해야 한다:

- 현재 기간 vs 이전 기간
- 브랜드/채널/제품 기여도
- 최근 베이스라인 대비 실질적 이탈

수십 개의 허영(vanity) 메트릭을 만들지 않는다. 모든 KPI는 경영상의 질문을 뒷받침해야 한다.

---

## 7. 의사결정 지원 레이어

애플리케이션은 차트에서 멈추지 않아야 한다.

실질적인 이슈에 대해, 다음과 같은 필드를 가진 구조화된 의사결정 항목을 만든다:

- 이슈
- 근거 KPI/사실
- 유력한 원인(들)
- 영향받는 브랜드/채널/SKU
- 결정적 규칙 기반의 심각도 또는 우선순위
- 가능한 조치
- 조치 후 모니터링할 KPI

단순한 결정적 규칙/이상탐지 레이어가 먼저 후보 이슈를 식별해야 한다.

그 다음 Gemini는:

- 근거를 요약하고
- 비즈니스적 함의를 설명하고
- 합리적인 조치 옵션을 제시하고
- 불확실성을 명시할 수 있다.

Gemini는 뒷받침되지 않는 사실을 만들어내서는 안 되며, 가능한 한 제한 없는 원본 데이터가 아니라 구조화된 메트릭/이슈 컨텍스트를 받아야 한다.

---

## 8. 경영진 애플리케이션

Cloud Run에 배포되는 컴팩트한 웹 애플리케이션을 만든다.

시각적 복잡도보다 정보 위계와 의사결정 유용성을 우선한다.

권장 최소 화면 구성:

### Overview
- 핵심 KPI 변화
- 가장 중요한 이슈들
- 간결한 경영진 요약

### Performance
- 브랜드/채널/제품별 매출/주문
- 유의미한 기간 비교

### Marketing
- 지출, ROAS, 획득 효율
- 유의미한 과소/과대 성과

### Customer
- 획득/재구매 고객 신호

### Inventory
- 품절 및 과잉재고 위험 후보

### Decisions
- 우선순위가 매겨진 이슈 카드
- 근거
- 제안 조치
- 사후 확인할 KPI

같은 로직을 더 적은 화면으로 더 잘 전달할 수 있다면 최종 정보 구조는 단순화해도 된다.

---

## 9. 합성 데이터 요구사항

합성 데이터는 다음을 만족해야 한다:

- 고정/랜덤 시드 옵션으로 재현 가능
- 해석 가능한 비즈니스 패턴을 만들어낼 만큼 현실적
- 애플리케이션이 탐지할 만한 의미 있는 내용을 갖도록, 알려진 시나리오/이상치를 의도적으로 포함
- 저렴하게 실행할 수 있을 만큼 작은 규모

시나리오 예시 유형:

- 마케팅 효율이 악화되는 브랜드/채널
- 수요는 늘고 있는데 재고가 얼마 남지 않은 제품
- 특정 세그먼트에 집중된 실질적 매출 하락
- 신규 vs 재구매 고객 비중의 유의미한 변화

생성기는 이러한 시나리오를 설정 가능하게 하거나, 최소한 명확히 문서화해야 한다.

---

## 10. 자동화

완성된 샘플은 반복 가능한 리프레시 흐름을 보여줘야 한다.

목표 흐름:

1. 합성 원본 데이터 생성/갱신
2. Cloud Storage에 원본 파일 배치
3. BigQuery 적재/갱신
4. 메트릭 재계산 또는 갱신
5. 갱신된 결과를 Cloud Run 앱에서 사용 가능하게 함
6. 필요에 따라 AI 요약 재생성

Cloud Scheduler가 매일 이 흐름을 트리거할 수 있다.

로컬 개발을 위해, 동일한 작업을 수행하는 간단한 수동 명령을 제공한다.

---

## 11. 엔지니어링 원칙

이것은 포트폴리오 샘플이지, 프로덕션 SaaS 제품이 아니다.

우선순위:

- 단순한 아키텍처
- 적은 의존성
- 명시적인 SQL과 비즈니스 규칙
- 재현 가능한 인프라
- 핵심 변환 로직에 대한 자동화된 테스트
- 명확한 문서화
- 낮은 GCP 비용
- 관측 가능한 실패 메시지

명백히 필요하지 않은 한 피할 것:

- 마이크로서비스
- Kubernetes
- 복잡한 이벤트 스트리밍
- 정교한 인증/권한 시스템
- 커스텀 ML 학습
- 불필요한 데이터베이스
- 과도하게 설계된 추상화
- 프로덕션 규모의 데이터 볼륨
- 의사결정 지원 스토리와 무관한 기능

---

## 12. 비용과 안전

이 프로젝트는 개발/시연 기간 동안 저렴하게 유지되도록 설계되어야 한다.

요구사항:

- 제한된 합성 데이터 볼륨
- 무한 루프나 고빈도 스케줄러 없음
- 불필요한 상시 구동 리소스 없음
- Git에 시크릿을 커밋하지 않음
- 파괴적인 클라우드 작업은 신중해야 함
- 프로젝트가 만든 인프라를 파기하는 방법을 문서화
- 포트폴리오 목적을 해치지 않는 선에서 무료/저비용 구성을 우선

---

## 13. 검증 요구사항

기능은 코드가 존재한다는 이유만으로 완료된 것이 아니다.

해당하는 범위에서 다음을 검증한다:

- 합성 데이터 생성 성공
- 기대한 시나리오/이상치가 생성된 데이터에 존재
- Cloud Storage 업로드 동작
- BigQuery 스키마/테이블 생성 성공
- 변환과 KPI 공식이 테스트를 통과
- 의사결정 규칙이 의도한 시나리오를 식별
- 애플리케이션이 로컬에서 실행됨
- 배포된 Cloud Run 엔드포인트가 응답함
- Gemini 출력이 제공된 메트릭 컨텍스트에 근거함
- 예약/수동 리프레시가 가시적인 결과를 갱신함
- 문서화된 단계로 인프라를 재생성할 수 있음

계산 로직과 핵심 파이프라인 동작에는 자동화된 테스트를 사용한다.

---

## 14. 포트폴리오 산출물

저장소는 다음을 갖춘 상태로 마무리되어야 한다:

- 동작하는 소스 코드
- 재현 가능한 인프라/배포 구성
- 샘플 합성 데이터셋 또는 생성 방법 안내
- 문서화된 KPI 정의
- 자동화된 테스트
- 다음을 포함한 README:
  - 문제 정의
  - 아키텍처
  - 의사결정 흐름
  - 셋업/배포
  - 스크린샷
  - 한계
  - 이 샘플의 예상/실측 GCP 비용
- 간단한 아키텍처 다이어그램
- 결정적 분석이 끝나고 Gemini의 해석이 시작되는 지점에 대한 명확한 설명

리뷰어가 실시간 구두 설명 없이도 저장소를 이해할 수 있어야 한다.

---

## 15. 완료 기준(Definition of Done)

리뷰어가 다음을 할 수 있을 때 이 프로젝트는 완료된 것이다:

1. 가상의 비즈니스 문제를 이해한다
2. 합성 데이터를 재현하거나 확인할 수 있다
3. 비즈니스 데이터가 BigQuery 메트릭으로 이어지는 과정을 추적할 수 있다
4. 경영 의사결정 애플리케이션을 연다
5. 데이터에서 탐지된, 의도적으로 심어진 비즈니스 이슈를 여러 개 확인한다
6. 각 이슈의 근거를 확인한다
7. 근거 있는 Gemini의 해석/요약을 본다
8. 시스템이 제안하는 조치와 후속 확인 KPI를 이해한다
9. 저장소 문서만으로 배포를 재현한다
10. 설계상의 트레이드오프와 한계를 이해한다

보기에는 그럴듯하지만 동작하지 않는 목업은 완료가 아니다.
기술적으로는 동작하지만 일관된 의사결정 스토리가 없는 파이프라인도 완료가 아니다.
